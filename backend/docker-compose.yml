version: "3.9"

services:
  backend:
    build: .
    container_name: luca_backend
    ports:
      - "3000:3000"
    environment:
      - GENIALLE_API=${GENIALLE_API}
    volumes:
      - ./uploads:/app/uploads
